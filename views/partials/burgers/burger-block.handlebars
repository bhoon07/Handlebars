<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Devoured!{{else}}Still Available!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
